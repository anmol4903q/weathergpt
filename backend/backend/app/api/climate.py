from fastapi import APIRouter

router = APIRouter(tags=["climate"])

# TODO: Phase-specific endpoints for climate go here.
# Not wired into main.py yet — add router.include_router(...) once implemented.
