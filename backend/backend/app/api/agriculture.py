from fastapi import APIRouter

router = APIRouter(tags=["agriculture"])

# TODO: Phase-specific endpoints for agriculture go here.
# Not wired into main.py yet — add router.include_router(...) once implemented.
